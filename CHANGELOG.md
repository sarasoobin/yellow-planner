# 변경 이력

이 파일의 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를,
버전 번호는 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따릅니다.

분류: `추가` · `변경` · `수정`(버그) · `제거` · `보안`

---

## [작업 중]

아직 없습니다.

---

## [1.0.0] - 2026-07-31

**처음으로 실제로 쓸 수 있는 판.** `PLAN.md`의 완료 기준 8가지를 모두 채웠습니다.
공개 URL로 접속되고, 계정별로 데이터가 격리되고, 핸드폰에서 레이아웃이 깨지지 않고,
빈 화면·로딩·에러 화면이 모두 있고, 빌드 경고가 0입니다.

### 추가

**노트 구조**
- 노트 껍데기 레이아웃 — 책상 위에 노란 공책이 놓인 구조, 모든 페이지 공통
- 인덱스 탭 14개 (표지 · 1~12월 · 메모). 데스크톱은 오른쪽 세로, 폰은 위쪽 가로 스크롤
- 월간 페이지 — 왼쪽 이 달 메모 + 오른쪽 달력, 중요 일정 색 띠, 할 일 개수 점,
  주차 이동 버튼 `›`
- 주간 페이지 — 월~일 7칸, 각 칸 위에 그 날 중요 일정, 아래 자잘한 할 일, 하단 주간 메모
- Free Note — 괘선 있는 자유 메모 한 장
- 표지에 월별 달성률 — 막대를 누르면 그 달로 이동
- 正자 집계 — 완료 5개마다 正 한 글자. 획을 순서대로 SVG로 그림

**적는 방법**
- 칸을 누르면 그 자리에서 바로 적힘. 별도 입력창이 열리지 않음
- `/` 를 치면 뜨는 메뉴 — 체크박스 · 펜 색 · 굵게 · 기울임 · 밑줄 · 형광펜 · 글자 크기
- 줄 맨 앞의 `# ` `## ` `### ` 를 글자 크기로 (26px / 20px / 16px+굵게)
- 고른 글자만 꾸미는 막대 — 글자를 긁으면 그 자리에 뜸
- 형광펜 세 자루 (분홍 · 초록 · 노랑). 같은 색으로 다시 그으면 지워지고
  다른 색으로 그으면 바뀜
- 밑줄 자 — 긁은 글자 아래에 빨간 줄
- 체크한 줄에 빨간 취소선

**스티커**
- 페이지 아무 자리에나 붙이고, 끌어서 옮기고, 모서리를 끌어 크기를 바꿈 (0.5~4배)
- 색연필로 그린 별(한붓그리기)과 동그라미. `public/stickers/` 에 직접 추가 가능

**빈 화면 · 로딩 · 에러**
- 노트 페이지 로딩 시 종이 모양 뼈대
- 에러 화면 3종 — 노트 안 / 랜딩·로그인 / 루트 레이아웃이 터졌을 때
- 없는 주소와 `notFound()` 를 함께 받는 화면
- 아무것도 안 적힌 칸에 안내 문구

**그 밖에**
- 가입 없이 둘러보는 데모 계정
- 파비콘 · 홈 화면 아이콘 (`正`). `npm run icons` 로 SVG 한 장에서 전부 생성
- 제목 · 로고 · 연도에 손글씨 폰트 (나눔손글씨 펜)

### 변경
- **한 칸을 통째로 적는 방식으로 바꿈.** 줄 목록이 아니라 종이 한 면처럼
  마구잡이로 쓰고, Enter로 줄을 바꾸고, 칸 끝에 닿으면 다음 줄로 이어짐
- 체크박스는 글자(☐ / ☑)로 넣음. 줄 앞이든 문장 중간이든 아무 자리에나 그릴 수 있음
- 체크 표시는 빨간 색연필로 그림. 네모를 넘어가도 둠 — 종이에 그으면 원래 그렇다
- **폰에서 달력 칸은 색 띠와 점만.** 375px 에서 한 칸이 43px 이라 13px 한글이
  두세 자밖에 안 들어갔다. 날짜를 누르면 달력 아래에서 그 날을 적는다
- 이 달 메모는 체크리스트 대신 자유롭게 적는 칸으로
- 로그인 · 랜딩 화면을 노트 테마로

### 수정
- 적던 글이 다시 그릴 때마다 처음 내용으로 되돌아가던 문제.
  React 19는 prop이 바뀌었는지 값이 아니라 참조로 판단하는데,
  `dangerouslySetInnerHTML` 에 매번 새 객체를 넘기고 있었다
- 주간에서 "달력으로" 가 엉뚱한 달로 가던 문제. 그 주 월요일이 속한 달로 갔는데,
  7월 1~5일에 적고 돌아오면 6월 달력이 열려 적은 게 사라진 것처럼 보였다
- 취소선이 이미 적어둔 글을 지나가지 않던 문제. 꾸미기 태그(`span`)를 새 줄로
  세고 있었다
- 형광펜이 한 번 칠하면 다시 눌러도 안 지워지던 문제. `hiliteColor` 는 껐다 켜는
  명령이 아니라 색을 넣는 명령이다
- 스티커를 놓으면 원래 자리로 되돌아갔다가 저장된 뒤 다시 가던 문제
- 폰에서 `/` 메뉴가 안 열리거나 키보드에 가리던 문제, 칸을 누를 때마다 화면이
  확대되던 문제
- 색을 여러 곳에 적어두어 한 곳만 고치면 어긋나던 문제

### 성능
- 스티커를 끌 때 뚝뚝 끊기던 것 — 크기 바꿀 때 매 프레임 SVG를 다시 그리던 것,
  손을 움직일 때마다 층 전체가 다시 그려지던 것, `left/top` 을 바꿔 자리를
  다시 계산하던 것 셋을 없앰

### 보안
- 적은 글을 HTML로 저장하므로, 저장 직전에 허용 목록에 없는 태그와 style을
  전부 걸러냄. 붙여넣기는 글자만 받음
- **RLS 교차 검증 완료.** 계정 A로 채운 표지·달력·주간·메모가 계정 B에서 모두
  비어 있고, A로 돌아오면 그대로 남아 있는 것을 확인

---

## [0.1.0] - 2026-07-27

Day 1. 로그인부터 항목 CRUD까지, 데이터가 실제로 저장되고 계정별로 격리되는 지점까지.

### 추가
- Supabase 연동 — 브라우저 / 서버 / 세션 갱신용 클라이언트 분리
- `proxy.ts` 기반 인증 가드 — 비로그인 시 `/cover`, `/month`, `/week`, `/note` 접근 차단
- 데이터베이스 스키마 (`profiles`, `items`, `notes`) 및 RLS 정책
- 가입 시 프로필을 자동 생성하는 트리거
- 이메일 회원가입 / 로그인 / 로그아웃
- 항목 CRUD Server Action — `createItem`, `toggleItem`, `updateItem`, `deleteItem`
- 표지 페이지 (`/cover`) — 올해의 목표 입력 및 달성 개수 표시
- 재사용 CRUD 컴포넌트 `ItemList` — `kind`와 `date`만 바꿔 월간 · 주간에서도 사용
- 기획 문서 `PRODUCT.md`, `DESIGN.md`

### 수정
- 항목 글자를 눌러도 수정 모드가 열리지 않던 문제
  StrictMode가 effect를 두 번 실행해 수정 모드가 열리자마자 닫히고 있었습니다.

### 보안
- 권한 검사를 앱이 아닌 DB(RLS)에 위임 — 앱 코드에 버그가 있어도 타인 데이터 접근 불가
- 세션 검증에 `getUser()` 사용 (`getSession()`은 쿠키를 그대로 신뢰해 위조 가능)
- 로그인 실패 메시지를 통일해 계정 존재 여부가 드러나지 않도록 처리
- `?next=` 파라미터를 내부 경로로 제한 — 오픈 리다이렉트 방지
- 입력값을 Zod로 검증하고 DB의 `check` 제약과 길이 제한을 일치시킴

---

## [0.0.1] - 2026-07-26

### 추가
- Next.js 16 프로젝트 초기 설정 (App Router, TypeScript, Tailwind CSS 4)
- GitHub 저장소 및 Vercel 자동 배포 연결

[작업 중]: https://github.com/sarasoobin/yellow-planner/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/sarasoobin/yellow-planner/compare/v0.1.0...v1.0.0
[0.1.0]: https://github.com/sarasoobin/yellow-planner/releases/tag/v0.1.0
[0.0.1]: https://github.com/sarasoobin/yellow-planner/commits/main
