# 디자인 결정서 (DESIGN.md)

> 작성일: 2026-07-26
> 근거: `docs/sketch-month-week.png` + 사용자 구두 설명
> **이 문서는 Claude Code가 스크린샷을 보고 대신 채운 것입니다. 읽어보고 "이건 아닌데" 싶은 것만 말씀해주세요.**

---

## 0. 이름 · 로고

**正 PLANNER** — 로고는 `正` 한 글자, 이름은 `PLANNER`.

```
헤더:    正  PLANNER
파비콘:  正
```

- 正에 설명을 붙이지 않는다. ("바를 정", "성장" 같은 부연을 넣는 순간 촌스러워짐)
- 正은 **5획 = 다섯 개**를 세는 표시. 이 앱에서 완료 개수를 세는 데 실제로 쓴다 (§5-3)
- 로고 색: 강조색 `#8B6F3D`, 손글씨 폰트 쓰면 여기에 씀

**아이콘** — 원본은 `src/app/icon.svg` 한 장. 여기서 `npm run icons` 로 나머지를 만들어냅니다.

| 파일 | 어디에 쓰이나 |
|---|---|
| `src/app/icon.svg` | 브라우저 탭 |
| `src/app/favicon.ico` | 주소창 옆·즐겨찾기 (SVG를 못 읽는 곳) |
| `src/app/apple-icon.png` | 아이폰 홈 화면 |
| `public/icon-192.png` · `icon-512.png` | 안드로이드 홈 화면·설치 화면 |
| `public/icon-maskable.png` | 안드로이드가 동그랗게 잘라내는 판 (글자를 작게 넣음) |

正의 획 좌표는 `src/components/tally-mark.tsx` 의 것과 같습니다. 로고의 正과
완료를 세는 正이 다르게 생기면 안 되기 때문입니다.

---

## 1. 컨셉 한 줄

**노란 표지의 아날로그 공책 한 권.** 앱을 쓰는 게 아니라 다이어리를 펼치는 느낌.

- 형용사: 따뜻한 / 아날로그한 / 차분한
- 절대 아닌 것: 앱스럽고 차가운 느낌, 알록달록한 대시보드, 그림자 남발

---

## 2. 색

> **진짜 값이 있는 곳은 `src/app/globals.css` 의 `@theme` 입니다.** 색을 바꿀 때는 거기서 바꾸시면 되고,
> 아래 표는 그 파일을 옮겨 적은 것입니다. 처음에 스크린샷에서 눈대중으로 뽑았던 값은 실제로 화면에 띄워보고
> 조정했기 때문에, 아래가 지금 화면에 보이는 색입니다.
> (globals.css 맨 위에 노란색 후보 12개가 주석으로 정리돼 있습니다. 골라서 두 줄만 바꾸면 됩니다.)

### 기본
| 역할 | 토큰 | 색 | 어디에 |
|---|---|---|---|
| 노트 표지·프레임 | `--color-frame` | `#FFF099` | 페이지 바깥 노란 테두리, 사이드 영역 |
| 프레임 테두리 | `--color-edge` | `#EBD97A` | 노란 영역의 1px 외곽선 |
| 종이 (페이지 배경) | `--color-paper` | `#FFFDF4` | 달력·주간칸 등 실제 내용이 올라가는 크림색 |
| 책상 (앱 배경) | `--color-desk` | `#F4F2ED` | 노트 바깥, 화면 가장자리 |
| 괘선·칸 구분선 | `--color-rule` | `#E4DAC0` | 주간 페이지 세로줄, 투두 밑줄 |
| 글자 (제목) | `--color-ink` | `#3A3226` | 짙은 먹빛 갈색 |
| 글자 (본문) | `--color-ink-soft` | `#5A5145` | |
| 글자 (보조·날짜) | `--color-ink-faint` | `#9A9080` | |

### 강조
| 역할 | 토큰 | 색 | 어디에 |
|---|---|---|---|
| 버튼·활성 상태 | `--color-accent` | `#8B6F3D` (짙은 겨자갈색) | 노란 배경 위에서도 확실히 보임 |
| 오늘 표시 | `--color-today` | `#C1453C` (빨간펜) | 달력의 오늘 날짜 동그라미 |
| 완료 체크 | `--color-done` | `#6B8E5A` (연한 잉크 초록) | 체크박스 |
| 삭제·위험 | `--color-danger` | `#B23B32` | |

> 노란 테마에서 가장 흔한 실패가 **"노란 배경에 노란 버튼"** 입니다. 그래서 버튼은 갈색 계열로 잡았습니다.

### 펜·형광펜 (글 안에 저장되는 색)

`/` 를 쳐서 고르는 글자 색입니다. 위 표와 달리 **토큰이 아니라 값을 직접 적어둡니다.**
적어둔 글에 색이 그대로 박혀 저장되는데, 저장할 때 걸러내는 규칙이 `#색상값` 만 통과시키기 때문입니다.

| 역할 | 색 | 어디에 적혀 있나 |
|---|---|---|
| 검정 펜 | `#3A3226` (= 글자 제목색) | `src/components/toolbar.tsx` |
| 빨강 펜 | `#C1453C` (= 오늘 표시색) | 〃 |
| 파랑 펜 | `#33618F` | 〃 — 짝이 되는 토큰이 없는 유일한 색 |
| 형광펜 | `#A8DE8A` (연둣빛 초록) | `src/lib/stickers.ts` |

> 형광펜은 처음에 노랑(`#F3ED7A`)이었습니다. 노트 표지도 노랑이라 도구 막대에 놓인
> 형광펜이 배경에 묻혔고, 크림색 종이 위에서도 칠한 자리가 잘 드러나지 않았습니다.
> 옛 노랑으로 칠해둔 글이 남아 있어서, **지우는 쪽은 두 색을 모두 알아봅니다.**

> 검정·빨강을 globals.css 에서 바꾸시면 이 두 파일도 같이 바꿔야 합니다. 두 곳뿐이라 주석으로 표시해뒀습니다.

### 12개월 인덱스 탭 색 (계절 따라)
```
1월 #A8C4D9   2월 #B8B0D4   3월 #F0BCC8   4월 #F5C9A0
5월 #C5DBA0   6월 #9CC9A8   7월 #8FC5D6   8월 #F5B889
9월 #D9B896   10월 #E09A73  11월 #B8A894  12월 #8FA3C4
```
- 표지 탭: 프레임 테두리색(`--color-edge`)을 그대로 따라갑니다. 값을 따로 적어두지 않았습니다 — 노란색을 바꿨을 때 이 탭만 옛 색으로 남는 일을 막기 위해서입니다.
- Free Note 탭: `#C9C4BC` (회색, 제일 아래)

> 7월이 하늘색인 이유는 여름이라서입니다. 순서가 마음에 안 들면 말씀해주세요.
> 12개월 색은 `src/lib/month-colors.ts` 에 있습니다.

### 다크모드
- [x] **안 만듭니다.** 종이 노트 컨셉과 정면으로 충돌합니다. 그 시간은 종이 질감 다듬기에 씁니다.

---

## 3. 글씨

| 용도 | 폰트 |
|---|---|
| 제목 ("7월", 표지 제목) | 손글씨 느낌 폰트 |
| 본문·투두·날짜 | Pretendard (한글 가독성) |

> **전체를 손글씨체로 하면 안 됩니다.** 투두를 여러 줄 적으면 못 읽습니다. 제목만 손글씨, 나머지는 깔끔하게 — 이게 아날로그 느낌을 내면서 실용성을 지키는 조합입니다.
> 후보: 나눔손글씨 계열 / 카페24 써라운드. Day 3에 두 개 띄워놓고 고르시면 됩니다.

---

## 4. 모양

| 항목 | 결정 | 이유 |
|---|---|---|
| 모서리 라운드 | **거의 없음 (4px)** | 종이는 각집니다. 둥글리면 앱스러워집니다 |
| 노트 전체 | 아주 옅은 그림자 | 책상 위에 공책이 놓인 느낌만 |
| 칸 구분 | 1px 선 | 공책 괘선 |
| 체크박스 | 네모, 채우지 않고 테두리만 | 종이에 그린 네모 느낌 |
| 체크 표시 | 빨간펜 색으로 **그린다.** 네모를 넘어가도 됨 | 색연필로 그은 느낌. 인쇄한 ☑ 는 종이 같지 않다 |

> 체크는 글 안의 한 글자(`☑`)라서 그 글자만 빨갛게 하거나 키울 수가 없습니다.
> CSS 로는 글자 하나를 집을 수 없기 때문입니다. 그래서 글자는 그대로 두고 그 자리를
> 재서 위에 표시를 그립니다 (`paper-block.tsx`). 획을 두 번 긋습니다 — 하나는 진하게,
> 하나는 살짝 비껴서 옅게. 색연필이 종이에 긁히며 남는 결입니다.

---

## 5. 화면 구조

### 5-0. 이 앱의 시각적 원칙 (제일 중요)

> **중요한 일정은 크고 진하게, 자잘한 할 일은 작고 연하게.**
> 이 대비가 무너지면 앱의 존재 이유가 사라집니다. (`PRODUCT.md` §2)

| | 중요한 일정 (달력) | 자잘한 할 일 (주간) |
|---|---|---|
| 글자 | 진하게, 색 있음 | 보통, 먹색 |
| 표시 | 색 띠 / 색 점 | 네모 체크박스 |
| 어디에 | 달력 칸 + 주간 페이지 **맨 위** | 주간 페이지 요일 칸 안 |

### 5-1. 공통 — 인덱스 탭
모든 페이지에 항상 붙어 있음. 14개: `표지` `1월` … `12월` `Free Note`

- 위치: **오른쪽 세로** ✅ 확정 (실제 노트 인덱스가 붙는 자리)
- 현재 보고 있는 달의 탭이 튀어나와 보이게 (활성 표시)
- 클릭하면 그 달로 즉시 이동

### 5-2. 표지 (로그인 후 첫 화면)
```
┌────────────────────────────────┬──┐
│                                │표│
│         2026                   │1 │
│                                │2 │
│   ┌────────────────────────┐   │3 │
│   │  올해의 목표           │   │4 │
│   │  □ ─────────────       │   │…│
│   │  □ ─────────────       │   │12│
│   │  □ ─────────────       │   │F │
│   └────────────────────────┘   │  │
└────────────────────────────────┴──┘
```

### 5-3. 월간 페이지 (스크린샷 위쪽)
```
┌──────────┬─────────────────────┬──┐
│  7월     │                     │표│
│          │                     │1 │
│ ┌──────┐ │       달  력        │…│
│ │이 달 │ │                     │7 │← 활성
│ │투두  │ │                     │…│
│ │□ ─── │ │                     │12│
│ │□ ─── │ │                     │F │
│ └──────┘ │                     │  │
│          │                     │  │
│ (여백)   │                     │  │
└──────────┴─────────────────────┴──┘
```
- 왼쪽 노란 단: 월 제목 + 그 달 할 일 + 아래 **正자 집계**
- 오른쪽 크림색: 달력

**正자 집계** — 스크린샷의 "여기엔 귀여운거 들어가면 조켓다" 자리
```
┌──────────┐
│ 正 正 正 │   ← 완료 5개마다 正 한 글자
│ 丅       │
│ 18개 완료│
└──────────┘
```
- 획을 순서대로 그린다: `一` → `丅` → `下`… → `正`
- 완료할 때마다 획이 하나 그어지는 짧은 애니메이션 (200ms)
- 손글씨 폰트를 쓴다면 여기가 제일 잘 어울리는 자리
- 색은 강조색 `#8B6F3D`

**달력 상세**
```
     월    화    수    목    금    토    일
┌──┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│› │ 13  │ 14  │ 15  │ 16  │ 17  │ 18  │ 19  │
│  │     │▮약속│     │     │     │     │     │
├──┼─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│› │ 20  │ 21  │ 22  │(23) │ 24  │ 25  │ 26  │
│  │ ●●  │     │     │▮시험│     │     │     │
└──┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
 ↑
 주차 버튼 — 누르면 그 주의 주간 페이지로
```

**클릭 규칙 (겹치지 않게 분리)**
| 어디를 누르나 | 무슨 일이 일어나나 |
|---|---|
| 왼쪽 `›` 버튼 | 그 주의 주간 페이지로 이동 |
| 칸 안 아무 데나 | 그 자리에서 바로 **일정 입력** (모달 없이, 종이에 적는 느낌) |
| 날짜 숫자 | 주간 페이지로 이동 (보조 경로 — 있어도 손해 없음) |

**한 칸 안**
```
┌──────────────┐
│ 23        ●● │  ← 날짜 / 그 날 자잘한 할 일 개수(점)
│ ▮ 중간고사   │  ← 중요 일정: 색 띠 + 제목 (진하게)
│ ▮ 과제 마감  │
│ +            │  ← 빈 곳 누르면 바로 입력
└──────────────┘
```
- 오늘 날짜는 빨간펜 색 동그라미
- 한 칸에 일정이 3개 넘으면 `+2개`로 접기
- 주차 버튼 폭: 데스크톱 28px / 모바일 24px (터치 세로 길이는 칸 높이만큼 확보되므로 충분)

### 5-4. 주간 페이지 (스크린샷 아래쪽)
```
┌────┬────┬────┬────┬────┬────┬────┬──┐
│ 월 │ 화 │ 수 │ 목 │ 금 │ 토 │ 일 │표│
│ 20 │ 21 │ 22 │ 23 │ 24 │ 25 │ 26 │1 │
│    │    │    │▮중간│    │    │    │…│  ← 그 날 중요 일정 (달력에서 가져옴)
│ ┄┄ │ ┄┄ │ ┄┄ │ 고사│ ┄┄ │ ┄┄ │ ┄┄ │7 │
│□목데│    │□강의│    │    │    │    │…│  ← 자잘한 할 일
│□장보│    │    │    │    │    │    │12│
├────┴────┴────┴────┴────┴────┴────┤F │
│                                   │  │
│         이번 주 메모              │  │
└───────────────────────────────────┴──┘
```
- 7칸 = 월~일
- **각 칸 맨 위에 그 날의 중요 일정**이 색 띠로 표시됨 (달력에 적은 것이 자동으로 올라옴, 여기선 수정 안 됨)
- 그 아래 얇은 구분선, 그 밑이 자잘한 할 일 영역
- 오늘 날짜 칸은 배경을 살짝 다르게
- 아래 넓은 칸 = 그 주 자유 메모

> 이 구조 덕분에 주간 페이지에서 일하다가도 "이번 주 수요일에 시험 있지"가 눈에 들어옵니다.

### 5-5. Free Note
줄 있는 빈 페이지 한 장. 자유롭게 적는 곳.

---

## 6. 핸드폰에서는 (반드시 다르게 만들어야 함)

> 이게 이 디자인의 **유일한 진짜 난관**입니다. 미리 정해둡니다.

| | 컴퓨터 | 핸드폰 (375px) |
|---|---|---|
| 인덱스 14개 | 오른쪽 세로 탭 | **위쪽 가로 스크롤 탭** (세로로는 절대 안 들어감) |
| 주간 7칸 | 가로 7열 | **세로로 7개 쌓기** (요일별 카드) |
| 월간 | 왼쪽 투두 + 오른쪽 달력 | 달력 위, 투두 아래 |
| 달력 한 칸 | 날짜 옆에 바로 적음 | **색 띠와 점만.** 누르면 달력 아래에서 그 날을 적음 |

> 핸드폰에서 7칸을 가로로 우겨넣으면 한 칸이 45px가 됩니다. 글자 두 자 들어갑니다. 세로로 쌓는 게 유일한 답입니다.

**달력 칸에서 글자를 뺀 이유** — 실제로 재보니 375px에서 한 칸이 43px, 글자가 놓이는
자리는 34px였습니다. 13px 한글이 한 줄에 두세 자 들어가서 "과제 마감 23:59"가
`과제 마 / 감 23:5 / 9` 로 잘렸습니다. 글자 크기를 줄여도 나아지지 않는 넓이라
칸에서 글자를 빼는 것 말고는 길이 없었습니다.

```
┌────────────────────────┐
│ 월 화 수 목 금 토 일   │
│ 13 14 15 16 17 18 19   │
│       ▀▀  ▀▀        ●● │  ← 색 띠 = 중요한 일정, 점 = 자잘한 할 일
├────────────────────────┤
│ 7월 15일 (수)   이 주 할 일│  ← 누른 날. 여기서 적고 고친다
│ 중간고사 (자료구조)      │
│ 범위: 3~7장             │
└────────────────────────┘
```

- 색 띠는 그 날 적어둔 줄 수만큼, 최대 3개까지
- 이 달에 오늘이 있으면 처음부터 오늘이 펴져 있음
- 컴퓨터 화면은 그대로입니다. 칸이 크니까 칸 안에서 바로 적는 편이 낫습니다

---

## 7. 정해진 것 / 남은 것

**정해진 것**
- [x] 인덱스 탭 → **오른쪽**
- [x] 달력 칸 → 날짜 + 중요 일정(색 띠) + 자잘한 할 일 개수(점)
- [x] 다크모드 → 안 만듦
- [x] 통계 → 표지에 월별 완료율

- [x] 달력 → 주간 이동 → **왼쪽 주차 버튼 `›`**, 칸 클릭은 일정 입력
- [x] 이름·로고 → **正 PLANNER**
- [x] 월간 페이지 왼쪽 아래 → **正자 집계**

- [x] 손글씨 폰트 → **나눔손글씨 펜 (Nanum Pen Script)**, 제목·로고·연도에만
      본문은 그대로 둔다. 할 일을 여러 줄 적었을 때 손글씨는 못 읽는다.
      쓰는 곳: 로고 `正`, 표지 연도와 "올해의 목표", 월 제목, 주 범위, Free Note

**남은 것 — 없음**

**이 문서를 쓴 뒤에 붙은 것**
이 아래는 처음 결정할 때 없던 기능입니다. 색과 모양은 위 규칙을 그대로 따릅니다.

- 스티커 — 위쪽 툴바에서 집어 페이지 아무 데나 붙임 (`public/stickers/`, 새로 넣는 법은 그 안의 README)
- 펜 색·굵게·기울임·형광펜 — 툴바가 아니라 **적던 자리에서 `/` 를 쳐서** 고름
      글을 적다 말고 화면 위로 올라가지 않게 하려는 것입니다
- 正자 집계는 §5-3 그대로 구현됨 (`src/components/tally-mark.tsx`)

---

## 8. 신경 안 쓰셔도 되는 것

정확한 픽셀 값, 글자 크기, 대비 검사, 로딩·빈 화면, 반응형 세부, 애니메이션 — 전부 제가 합니다.
